[build]
  publish = "dist/public"
  command = "npm install && npx vite build --config vite.config.netlify.ts"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
